import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://bridgenta.de',
  // Enable static site generation (default in Astro)
  output: 'static'
});
